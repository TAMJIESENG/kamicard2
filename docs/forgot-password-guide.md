# 忘记密码功能使用指南

## 功能概述

全新的"忘记密码"功能允许用户通过邮箱验证来重置密码，管理员可以在后台查看和管理所有的密码重置请求。

## 用户端使用流程

### 步骤 1：访问忘记密码页面

1. 在登录页面点击"忘记密码？"链接
2. 或直接访问：`/forgot-password`

### 步骤 2：验证身份

输入以下信息：
- **用户名**：您注册时使用的用户名
- **注册邮箱**：您注册时使用的邮箱地址

点击"获取验证码"按钮

系统会验证：
- ✅ 用户名和邮箱是否匹配
- ✅ 用户是否存在

### 步骤 3：接收验证码

- 验证码将发送到您的邮箱（**开发环境下，验证码会直接显示在页面提示中**）
- 验证码为 **6位数字**
- 有效期：**10分钟**

### 步骤 4：重置密码

输入：
1. **验证码**（6位数字）
2. **新密码**（至少6个字符）
3. **确认新密码**（需与新密码一致）

密码强度提示：
- 🔴 弱：少于8个字符或简单组合
- 🟡 中：8-12个字符，包含字母和数字
- 🟢 强：12个字符以上，包含大小写字母、数字和特殊字符

### 步骤 5：完成重置

密码重置成功后：
- ✅ 系统显示成功提示
- 🔐 立即使用新密码登录

## 管理员后台功能

### 访问密码重置管理

1. 以管理员身份登录
2. 进入管理后台
3. 点击侧边栏"密码重置"菜单

### 功能概览

#### 📊 统计面板

显示四个关键指标：
- **总重置请求**：所有密码重置请求总数
- **已使用**：已成功重置密码的请求数
- **待使用**：未使用且未过期的请求数
- **已过期**：超过有效期的请求数

#### 📋 当前有效请求

如果有待使用的重置请求，会显示：
- 用户名
- 邮箱（部分隐藏）
- **验证码**（管理员可复制）
- 过期时间
- 剩余时间（实时倒计时）

#### 📜 历史记录表格

查看所有密码重置记录：
- 用户名
- 邮箱
- 验证码
- 状态（待使用/已使用/已过期）
- 请求时间
- 过期时间
- 剩余时间

#### 🛠️ 管理操作

**刷新按钮**
- 手动刷新数据
- 更新最新的重置请求信息

**清理过期记录**
- 一键删除所有已过期的重置请求
- 保持后台数据整洁

**查看详情**
- 点击每条记录的"详情"按钮
- 查看完整的请求信息
- 复制验证码

**删除记录**
- 删除单条重置请求记录
- 确认后永久删除

## 技术实现细节

### 数据存储

#### 当前重置请求
```javascript
// LocalStorage Key: password_reset_data
{
  username: "user123",
  email: "user@example.com",
  code: "123456",
  expireTime: 1234567890000,
  used: false,
  timestamp: 1234567890000
}
```

#### 系统日志
```javascript
// LocalStorage Key: system_logs
{
  id: 1234567890,
  type: 'password_reset',
  level: 'info',
  message: '用户 user123 通过邮箱验证重置了密码',
  username: 'user123',
  timestamp: '2025-10-18T12:00:00.000Z',
  details: {
    email: 'user@example.com',
    method: 'email_verification'
  }
}
```

### 验证机制

#### 1. 身份验证
- 验证用户名和邮箱是否匹配
- 查询本地用户数据库（`all_users`）
- 匹配失败返回错误提示

#### 2. 验证码生成
```javascript
// 6位随机数字
const code = Math.random().toString().slice(2, 8).padStart(6, '0')
```

#### 3. 有效期控制
- 生成时间戳：`Date.now()`
- 过期时间：`Date.now() + 10 * 60 * 1000` (10分钟)
- 实时验证：`Date.now() > expireTime`

#### 4. 验证码验证
检查项：
- ✅ 验证码是否正确
- ✅ 验证码是否过期
- ✅ 验证码是否已使用
- ✅ 用户信息是否匹配

#### 5. 密码更新
```javascript
// 更新用户密码
allUsers[userIndex].password = newPassword

// 标记验证码为已使用
resetData.used = true

// 记录系统日志
logs.unshift({
  type: 'password_reset',
  username: username,
  timestamp: new Date().toISOString()
})
```

### 安全特性

1. **一次性验证码**
   - 每个验证码只能使用一次
   - 使用后自动标记为已使用

2. **时间限制**
   - 10分钟有效期
   - 超时自动失效

3. **重发限制**
   - 60秒倒计时
   - 防止频繁发送

4. **信息脱敏**
   - 邮箱部分隐藏显示
   - 保护用户隐私

5. **日志记录**
   - 所有密码重置操作记录日志
   - 方便审计和追踪

## 注意事项

### 用户端

⚠️ **验证码有效期只有10分钟**
- 请尽快完成密码重置
- 超时需要重新获取验证码

⚠️ **密码强度建议**
- 使用至少8个字符
- 包含大小写字母、数字和特殊字符
- 不要使用常见密码

⚠️ **一次只能有一个有效的重置请求**
- 新的请求会覆盖旧的请求
- 请确保及时使用验证码

### 管理员端

⚠️ **验证码敏感信息**
- 验证码属于敏感信息
- 不要泄露给第三方
- 建议只在必要时查看

⚠️ **定期清理过期记录**
- 保持后台数据整洁
- 提高系统性能

⚠️ **监控异常请求**
- 注意频繁的重置请求
- 可能存在恶意行为

## 常见问题

### Q1: 收不到验证码怎么办？
**A:** 开发环境下，验证码会直接显示在成功提示中。生产环境需要配置邮件服务。

### Q2: 验证码过期了怎么办？
**A:** 返回第一步，重新获取新的验证码。

### Q3: 忘记用户名怎么办？
**A:** 请联系管理员帮助找回用户名。

### Q4: 邮箱不是注册时的邮箱？
**A:** 请联系管理员更新邮箱信息。

### Q5: 可以为他人重置密码吗？
**A:** 不可以。每个用户只能重置自己的密码。管理员可以在后台强制重置。

### Q6: 重置密码后需要重新登录吗？
**A:** 是的，使用新密码登录所有设备。

## 未来改进计划

### 邮件功能
- [ ] 集成真实邮件服务（SMTP）
- [ ] 美化邮件模板
- [ ] 支持邮件追踪

### 安全增强
- [ ] 添加图形验证码（防止机器人）
- [ ] IP地址限制（防止暴力破解）
- [ ] 多因素认证（短信+邮箱）

### 用户体验
- [ ] 记住上次输入的用户名
- [ ] 自动填充已知邮箱
- [ ] 进度自动保存

### 管理功能
- [ ] 批量操作（批量删除/导出）
- [ ] 数据统计图表
- [ ] 异常行为预警

## 开发者接口

### 前端路由

```javascript
{
  path: '/forgot-password',
  name: 'ForgotPassword',
  component: () => import('@/views/ForgotPassword.vue')
}
```

### 核心组件

- `ForgotPassword.vue` - 用户端密码重置页面
- `PasswordResetManagement.vue` - 管理员后台管理组件

### 数据模型

```typescript
interface PasswordResetData {
  username: string
  email: string
  code: string
  expireTime: number
  used: boolean
  timestamp: number
}
```

---

**最后更新**: 2025-10-18  
**版本**: v1.0.0  
**作者**: AI Assistant

